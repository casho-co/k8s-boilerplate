const PRODUCER = 'producer';
const CONSUMER = 'consumer';

export enum TOPICS {
  HEALTH = 'health',
}

export enum HEALTH_EVENTS {
  HEALTH_CHECK = 'health_check',
}

// const config: any = {};

// config[PRODUCER][TOPICS.HEALTH] = [HEALTH_EVENTS.HEALTH_CHECK];
// config[CONSUMER][TOPICS.HEALTH] = [HEALTH_EVENTS.HEALTH_CHECK];

// export default config;
